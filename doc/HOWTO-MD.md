# Project Documentation

Welcome to the project! Here is a guide on how to get started.

## 1. Prerequisites
Make sure you have installed the following:
* Node.js (version 18.0 or higher)
* NPM (version 9.0 or higher)

## 2. Installation
To install the dependencies, run the following command in your terminal:

```bash
npm install
```

## 3. Configuration
Set the following environment variables in your `.env` file:

| Variable | Description | Default |
| :--- | :--- | :--- |
| `PORT` | The port the server listens to | `3000` |
| `HOST` | The server host address | `localhost` |

## 4. Usage
To start the application, use the following command:

```javascript
const app = require('./app');
app.start();
```

> [!TIP]
> Always check the [Official Documentation](https://www.markdownguide.org/basic-syntax/) to explore more advanced Markdown features.

